{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "Herald Client",
  "description": "Herald automation HTTP bridge (client-side, single-end).",
  "authors": ["Herald Team"],
  "contact": {},
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "client": [
      "ai.herald.clientmod.fabric.HeraldClientFabric"
    ]
  },
  "mixins": [
    "herald-client.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.15.0",
    "minecraft": "~${minecraft_version}",
    "java": ">=17",
    "fabric-api": "*"
  }
}
